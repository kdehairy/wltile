# Minimal Sway config for headless integration testing.
# Overrides the default config entirely so no keybindings, bar, or input
# setup is attempted.

xwayland disable

# Suppress the default background — swaybg is not installed in the test image.
output * bg #000000 solid_color
